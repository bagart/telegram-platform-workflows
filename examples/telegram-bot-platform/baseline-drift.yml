name: Baseline Drift

on:
  schedule:
    - cron: '9 4 * * 1' # weekly, Monday
  workflow_dispatch:

permissions:
  contents: read

jobs:
  drift:
    uses: BAGArt/telegram-platform-workflows/.github/workflows/baseline-drift.yml@<CENTRAL_SHA> # pin!
