name: Security

on:
  push:
    branches:
      - develop
      - main
      - master
      - workos
  pull_request:
  schedule:
    # Weekly dependency/secret sweep — cheap, no container builds (cost guard).
    - cron: '23 4 * * 1'

permissions:
  contents: read

jobs:
  security:
    uses: BAGArt/telegram-platform-workflows/.github/workflows/security.yml@<CENTRAL_SHA> # pin!
    permissions:
      contents: read
      security-events: write # SARIF upload when export-sarif is enabled
    with:
      php-version: '8.5'
      export-sarif: true
